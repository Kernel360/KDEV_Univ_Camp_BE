spring:
  datasource:
    url: jdbc:mysql://todo-car.cv8eyc8867ij.ap-northeast-2.rds.amazonaws.com:3306/todo-car
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: 1q2w3e4r!!

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update  # 기존 데이터 유지
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    defer-datasource-initialization: true  # data.sql을 실행하도록 설정
