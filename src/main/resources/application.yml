spring:
  datasource:
    url: jdbc:mysql://todo-car.cv8eyc8867ij.ap-northeast-2.rds.amazonaws.com:3306/todo-car
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: 1q2w3e4r!!

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect  # ✅ MySQL8Dialect 대신 최신 버전 사용
    hibernate:
      ddl-auto: update  # ✅ 기존 테이블 유지하며 새로운 테이블 추가 가능하게 변경
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    defer-datasource-initialization: true  # ✅ data.sql 실행 보장

  sql:
    init:
      mode: always  # ✅ Hibernate 실행 후 data.sql 실행

  security:
    user:
      name: user
      password: password

jwt:
  header: Authorization
  secret: c2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQtc2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQK
  token-validity-in-seconds: 86400

server:
  port: 8080

logging:
  level:
    root: INFO
    me.silvernine: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
